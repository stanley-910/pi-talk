{
  "name": "pi-talk",
  "version": "0.0.0",
  "private": true,
  "description": "Pi extension for OpenAI text-to-speech",
  "type": "module",
  "scripts": {
    "start": "pi -e ./src/index.ts",
    "test": "node --test test/*.test.ts"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
